import React from "react";
import Spain from "./Spain";


function SpainContainer(props) {
    return(<div>
        <Spain name={props.name} url={props.url}/>
    </div>)
}

export default SpainContainer;